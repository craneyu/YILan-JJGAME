services:
  frontend:
    image: yilan-jju-frontend
    platform: linux/arm64
    ports:
      - "4200:80"
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    image: yilan-jju-backend
    platform: linux/arm64
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/jju
      - JWT_SECRET=jju_secret_change_this
      - PORT=3000
      - NODE_ENV=production
    depends_on:
      mongo:
        condition: service_healthy
    restart: unless-stopped
    mem_limit: 400m

  mongo:
    image: mongo:7
    platform: linux/arm64
    command: ["--wiredTigerCacheSizeGB", "0.25"]
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    mem_limit: 512m

volumes:
  mongo_data:
